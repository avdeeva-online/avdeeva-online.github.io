(()=>{
  /* Compatibility shim.
     Modal geometry and visual hierarchy are now owned by modal-carddata-ux.js.
     Keeping this file loaded avoids cache/deploy surprises while removing
     the old competing !important rules. */
})();