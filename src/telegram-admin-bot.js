import { analyzeTelegramPost } from './hub-telegram.js';
import { publishHubResource } from './hub-resources.js';

const json=(data,status=200)=>new Response(JSON.stringify(data),{status,headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store'}});
const clean=v=>String(v??'').trim();
const esc=s=>clean(s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
const short=(s,n=420)=>{s=clean(s).replace(/\s+/g,' ');return s.length>n?s.slice(0,n-1)+'…':s};
const uniqBy=(items,key)=>{const seen=new Set();return (items||[]).filter(x=>{const k=key(x);if(!k||seen.has(k))return false;seen.add(k);return true})};

async function sha256Hex(value){const bytes=new TextEncoder().encode(value),hash=await crypto.subtle.digest('SHA-256',bytes);return[...new Uint8Array(hash)].map(x=>x.toString(16).padStart(2,'0')).join('')}
async function webhookSecret(token){return(await sha256Hex(`admin|${token}`)).slice(0,48)}
async function tg(token,method,payload={}){const r=await fetch(`https://api.telegram.org/bot${token}/${method}`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)}),d=await r.json().catch(()=>({ok:false,description:`HTTP_${r.status}`}));if(!r.ok||!d.ok)throw new Error(d.description||`TELEGRAM_${method}_FAILED`);return d.result}
const button=(text,callback_data)=>({text,callback_data});
const urlButton=(text,url)=>({text,url});
async function send(token,chatId,text,reply_markup){return tg(token,'sendMessage',{chat_id:chatId,text,parse_mode:'HTML',disable_web_page_preview:true,...(reply_markup?{reply_markup}:{})})}
async function edit(token,chatId,messageId,text,reply_markup){try{return await tg(token,'editMessageText',{chat_id:chatId,message_id:messageId,text,parse_mode:'HTML',disable_web_page_preview:true,...(reply_markup?{reply_markup}:{})})}catch{return send(token,chatId,text,reply_markup)}}
async function answerCb(token,id,text=''){try{await tg(token,'answerCallbackQuery',{callback_query_id:id,...(text?{text}:{})})}catch{}}

function adminMenu(){return{inline_keyboard:[
  [button('＋ IMPORT','adm:import'),button('DRAFTS','adm:drafts')],
  [button('SUGGESTIONS','adm:suggestions'),button('HUB','adm:hub')],
  [button('AUTHORS','adm:authors'),button('ISSUES','adm:issues')],
  [button('STATS','adm:stats')]
]}}
function activeButtons(id,source){return{inline_keyboard:[
  [button('✓ FINISH RESOURCE','session:finish'),button('＋ NEW RESOURCE','session:new')],
  [button('RE-ANALYZE','draft:reanalyze:'+id),button('DELETE','draft:delete:'+id)],
  [source?urlButton('ORIGINAL POST ↗',source):button('SOURCE','noop'),button('DRAFTS','adm:drafts')],
  [button('HOME','adm:home')]
]}}
function idleDraftButtons(id,source){return{inline_keyboard:[
  [button('CONTINUE THIS RESOURCE','session:resume:'+id),button('PUBLISH','draft:publish:'+id)],
  [button('RE-ANALYZE','draft:reanalyze:'+id),button('DELETE','draft:delete:'+id)],
  [source?urlButton('ORIGINAL POST ↗',source):button('SOURCE','noop'),button('← DRAFTS','adm:drafts')],
  [button('HOME','adm:home')]
]}}

async function ensureSchema(env){
  await env.DB.prepare(`CREATE TABLE IF NOT EXISTS telegram_admin_drafts(id TEXT PRIMARY KEY,source_url TEXT NOT NULL UNIQUE,payload TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'review',created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)`).run();
  await env.DB.prepare(`CREATE TABLE IF NOT EXISTS telegram_admin_import_session(admin_user_id TEXT PRIMARY KEY,channel TEXT NOT NULL DEFAULT '',draft_id TEXT NOT NULL DEFAULT '',last_post_id INTEGER NOT NULL DEFAULT 0,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)`).run();
}
async function analyzeUrl(url){const r=await analyzeTelegramPost(new Request('https://internal/api/admin/hub-telegram-analyze',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({url})})),d=await r.json();if(!r.ok||!d.ok)throw new Error(d.message||d.error||'ANALYZE_FAILED');return d}
function forwardedMeta(message){const o=message?.forward_origin;if(o?.type==='channel'&&o.chat?.username&&o.message_id)return{channel:o.chat.username,postId:Number(o.message_id),url:`https://t.me/${o.chat.username}/${o.message_id}`};const l=message?.forward_from_chat;if(l?.username&&message?.forward_from_message_id)return{channel:l.username,postId:Number(message.forward_from_message_id),url:`https://t.me/${l.username}/${message.forward_from_message_id}`};return null}
function extractTelegramUrl(text){return clean(text).match(/https?:\/\/t\.me\/(?:s\/)?[A-Za-z0-9_]+\/\d+/i)?.[0]||''}
function parseSourceUrl(raw){try{const u=new URL(raw),m=u.pathname.replace(/^\/s\//,'/').match(/^\/([^/]+)\/(\d+)/);return m?{channel:m[1],postId:Number(m[2]),url:`https://t.me/${m[1]}/${m[2]}`} : null}catch{return null}}
function directFiles(message,sourceUrl=''){const out=[];if(message?.document?.file_id)out.push({telegram_file_id:message.document.file_id,telegram_file_unique_id:message.document.file_unique_id||'',name:message.document.file_name||`telegram-file-${message.message_id}`,size:Number(message.document.file_size||0),type:message.document.mime_type||'application/octet-stream',source:'telegram-bot',source_post:sourceUrl});if(message?.audio?.file_id)out.push({telegram_file_id:message.audio.file_id,telegram_file_unique_id:message.audio.file_unique_id||'',name:message.audio.file_name||`audio-${message.message_id}.mp3`,size:Number(message.audio.file_size||0),type:message.audio.mime_type||'audio/mpeg',source:'telegram-bot',source_post:sourceUrl});if(message?.video?.file_id)out.push({telegram_file_id:message.video.file_id,telegram_file_unique_id:message.video.file_unique_id||'',name:message.video.file_name||`video-${message.message_id}.mp4`,size:Number(message.video.file_size||0),type:message.video.mime_type||'video/mp4',source:'telegram-bot',source_post:sourceUrl});return out}
function directMedia(message,sourceUrl=''){const photos=Array.isArray(message?.photo)?message.photo:[];if(!photos.length)return[];const p=photos[photos.length-1];return[{telegram_file_id:p.file_id,telegram_file_unique_id:p.file_unique_id||'',size:Number(p.file_size||0),width:p.width||0,height:p.height||0,source:'telegram-bot-photo',source_post:sourceUrl}]}
function applyChannelIdentity(a,channel){if(!a||!channel)return a;a.source=a.source||{};a.source.channel=a.source.channel||channel;a.source.author=a.source.author||channel;a.draft=a.draft||{};a.draft.creator=a.draft.creator||channel;a.draft.creatorLink=a.draft.creatorLink||`https://t.me/${channel}`;return a}
async function saveDraft(env,analysis){const source=clean(analysis?.source?.url);if(!source)throw new Error('SOURCE_URL_MISSING');const existing=await env.DB.prepare('SELECT id FROM telegram_admin_drafts WHERE source_url=? LIMIT 1').bind(source).first();if(existing?.id){await env.DB.prepare("UPDATE telegram_admin_drafts SET payload=?,status='review',updated_at=CURRENT_TIMESTAMP WHERE id=?").bind(JSON.stringify(analysis),existing.id).run();return existing.id}const id=crypto.randomUUID();await env.DB.prepare("INSERT INTO telegram_admin_drafts(id,source_url,payload,status) VALUES(?,?,?,'review')").bind(id,source,JSON.stringify(analysis)).run();return id}
async function getDraft(env,id){const row=await env.DB.prepare('SELECT * FROM telegram_admin_drafts WHERE id=? LIMIT 1').bind(id).first();if(!row)return null;try{row.analysis=JSON.parse(row.payload||'{}')}catch{row.analysis={}}return row}
async function getSession(env,userId){return env.DB.prepare('SELECT * FROM telegram_admin_import_session WHERE admin_user_id=? LIMIT 1').bind(String(userId)).first()}
async function setSession(env,userId,draftId,channel='',postId=0){await env.DB.prepare(`INSERT INTO telegram_admin_import_session(admin_user_id,channel,draft_id,last_post_id,updated_at) VALUES(?,?,?,?,CURRENT_TIMESTAMP) ON CONFLICT(admin_user_id) DO UPDATE SET channel=excluded.channel,draft_id=excluded.draft_id,last_post_id=excluded.last_post_id,updated_at=CURRENT_TIMESTAMP`).bind(String(userId),channel||'',draftId||'',Number(postId||0)).run()}
async function clearSession(env,userId){await env.DB.prepare('DELETE FROM telegram_admin_import_session WHERE admin_user_id=?').bind(String(userId)).run()}
function hasDirectAttachment(message){return Boolean(message?.document||message?.audio||message?.video||message?.photo?.length)}
function draftText(row,active=false){const a=row.analysis||{},d=a.draft||{},posts=(a.sourcePosts||[]).length||1;return `${active?'<b>ACTIVE RESOURCE · SEND MORE POSTS/FILES</b>\n\n':''}<b>DRAFT RESOURCE</b>\n\n<b>${esc(d.title||'UNTITLED')}</b>\nTYPE: ${esc(d.type||'other')}\nAUTHOR: ${esc(d.creator||a.source?.channel||'—')}\nSOURCE POSTS: ${posts}\nMEDIA: ${(a.media||[]).length} · FILES: ${(a.files||[]).length}\nSOURCE: ${esc(a.source?.url||row.source_url)}\n\n${esc(short(d.descriptionShort||d.descriptionFull||'',260))}`}

async function mergeIntoDraft(env,row,incoming,message,meta){
  const base=row.analysis||{},inc=incoming||{};base.source=base.source||{};base.draft=base.draft||{};
  const channel=base.source.channel||meta?.channel||inc.source?.channel||'';applyChannelIdentity(base,channel);
  const primary=clean(base.source.url||row.source_url||meta?.url);base.source.url=primary;
  const urls=[];for(const x of [...(base.sourcePosts||[]),meta?.url?{url:meta.url}:null,inc.source?.url?{url:inc.source.url}:null]){const u=typeof x==='string'?x:clean(x?.url);if(u&&!urls.some(y=>y.url===u))urls.push({url:u})}base.sourcePosts=urls;
  const incText=clean(inc.source?.rawText),baseText=clean(base.source?.rawText);if(incText&&incText.length>baseText.length){base.source.rawText=incText;base.draft.title=inc.draft?.title||base.draft.title;base.draft.descriptionShort=inc.draft?.descriptionShort||base.draft.descriptionShort;base.draft.descriptionFull=inc.draft?.descriptionFull||incText;base.draft.type=inc.draft?.type||base.draft.type;base.draft.models=inc.draft?.models||base.draft.models;base.draft.settings=inc.draft?.settings||base.draft.settings}
  base.media=uniqBy([...(base.media||[]),...(inc.media||[]),...directMedia(message,meta?.url||'')],x=>x.url||x.telegram_file_unique_id||x.telegram_file_id);
  base.files=uniqBy([...(base.files||[]),...(inc.files||[]),...directFiles(message,meta?.url||'')],x=>x.telegram_file_unique_id||x.telegram_file_id||x.url||`${x.name}:${x.size}`);
  base.diagnostics={...(base.diagnostics||{}),sourcePosts:base.sourcePosts.length,directFiles:base.files.filter(x=>x.telegram_file_id).length,explicitSession:true};
  await env.DB.prepare("UPDATE telegram_admin_drafts SET payload=?,status='review',updated_at=CURRENT_TIMESTAMP WHERE id=?").bind(JSON.stringify(base),row.id).run();return getDraft(env,row.id)
}
async function attachRawFiles(env,row,message){const a=row.analysis||{};a.media=uniqBy([...(a.media||[]),...directMedia(message,'')],x=>x.telegram_file_unique_id||x.telegram_file_id||x.url);a.files=uniqBy([...(a.files||[]),...directFiles(message,'')],x=>x.telegram_file_unique_id||x.telegram_file_id||x.url||`${x.name}:${x.size}`);a.diagnostics={...(a.diagnostics||{}),manualAttachments:true,directFiles:a.files.filter(x=>x.telegram_file_id).length};await env.DB.prepare("UPDATE telegram_admin_drafts SET payload=?,status='review',updated_at=CURRENT_TIMESTAMP WHERE id=?").bind(JSON.stringify(a),row.id).run();return getDraft(env,row.id)}

async function downloadTelegramFile(token,file){const info=await tg(token,'getFile',{file_id:file.telegram_file_id});if(!info?.file_path)throw new Error('TELEGRAM_FILE_PATH_MISSING');const r=await fetch(`https://api.telegram.org/file/bot${token}/${info.file_path}`);if(!r.ok)throw new Error(`TELEGRAM_FILE_DOWNLOAD_${r.status}`);return new File([await r.arrayBuffer()],file.name||info.file_path.split('/').pop()||'telegram-file',{type:file.type||r.headers.get('content-type')||'application/octet-stream'})}
async function publishDraft(token,env,row){const a=row.analysis||{},d=a.draft||{},channel=clean(a.source?.channel),all=Array.isArray(a.files)?a.files:[],bot=all.filter(f=>f.telegram_file_id),remote=all.filter(f=>f.url&&!f.telegram_file_id),payload={source:{type:'telegram',url:a.source?.url||row.source_url},type:clean(d.type)||'other',title:clean(d.title)||'UNTITLED RESOURCE',creator:{name:channel||clean(d.creator||a.source?.author),link:channel?`https://t.me/${channel}`:clean(d.creatorLink)},description_short:clean(d.descriptionShort),description_full:clean(d.descriptionFull||a.source?.rawText),additional_info:'',models:Array.isArray(d.models)?d.models:[],settings:Array.isArray(d.settings)?d.settings:[],tags:Array.isArray(d.tags)?d.tags:[],media:Array.isArray(a.media)?a.media.filter(x=>x.url):[],confidence:a.confidence||{}};let req;if(bot.length){const form=new FormData();form.append('resource',new Blob([JSON.stringify(payload)],{type:'application/json'}),'resource.json');for(const f of bot)form.append('files',await downloadTelegramFile(token,f),f.name||'telegram-file');for(const f of remote)form.append('remoteFiles',JSON.stringify({...f,source:'telegram'}));req=new Request('https://internal/api/admin/hub-resource',{method:'POST',body:form})}else{payload.files=remote.map(f=>({...f,source:'telegram'}));req=new Request('https://internal/api/admin/hub-resource',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)})}const r=await publishHubResource(req,env),out=await r.json();if(!r.ok||!out.ok)throw new Error(out.detail||out.error||'PUBLISH_FAILED');await env.DB.prepare("UPDATE telegram_admin_drafts SET status='published',updated_at=CURRENT_TIMESTAMP WHERE id=?").bind(row.id).run();return out}

async function adminDrafts(env){return(await env.DB.prepare("SELECT id,source_url,payload,status,updated_at FROM telegram_admin_drafts WHERE status='review' ORDER BY updated_at DESC LIMIT 12").all()).results||[]}
async function showDraftList(token,chatId,messageId,env){const rows=await adminDrafts(env);if(!rows.length)return edit(token,chatId,messageId,'<b>DRAFTS</b>\n\nNo drafts waiting for review.',{inline_keyboard:[[button('＋ IMPORT','adm:import'),button('HOME','adm:home')]]});const keys=[];for(const r of rows){let title='UNTITLED';try{title=JSON.parse(r.payload||'{}')?.draft?.title||title}catch{}keys.push([button(short(title,36),'draft:view:'+r.id)])}keys.push([button('＋ IMPORT','adm:import'),button('HOME','adm:home')]);return edit(token,chatId,messageId,`<b>DRAFTS</b>\n\n${rows.length} most recent drafts waiting for review.`,{inline_keyboard:keys})}
async function adminStats(env){const one=async sql=>{try{return Number((await env.DB.prepare(sql).first())?.n||0)}catch{return 0}};const[resources,drafts,suggestions,characters,files]=await Promise.all([one("SELECT COUNT(*) n FROM hub_resources WHERE status='published'"),one("SELECT COUNT(*) n FROM telegram_admin_drafts WHERE status='review'"),one("SELECT COUNT(*) n FROM hub_suggestions WHERE status='new'"),one('SELECT COUNT(*) n FROM characters'),one('SELECT COUNT(*) n FROM hub_resource_files')]);return `<b>ARCHIVE.EXE / STATS</b>\n\nHUB RESOURCES: ${resources}\nDRAFTS: ${drafts}\nNEW SUGGESTIONS: ${suggestions}\nBOT CARDS: ${characters}\nHUB FILES: ${files}`}
async function showSuggestions(token,chatId,messageId,env){try{await env.DB.prepare(`CREATE TABLE IF NOT EXISTS hub_suggestions(id INTEGER PRIMARY KEY AUTOINCREMENT,url TEXT NOT NULL,note TEXT NOT NULL DEFAULT '',status TEXT NOT NULL DEFAULT 'new',created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)`).run()}catch{}const rows=(await env.DB.prepare("SELECT id,url,note FROM hub_suggestions WHERE status='new' ORDER BY created_at DESC LIMIT 6").all()).results||[];if(!rows.length)return edit(token,chatId,messageId,'<b>COMMUNITY SUGGESTIONS</b>\n\nInbox is empty.',{inline_keyboard:[[button('HOME','adm:home')]]});const keys=rows.map(r=>[button('IMPORT #'+r.id,'sug:import:'+r.id),button('IGNORE','sug:ignore:'+r.id)]);keys.push([button('HOME','adm:home')]);return edit(token,chatId,messageId,'<b>COMMUNITY SUGGESTIONS</b>\n\n'+rows.map(r=>`#${r.id} · ${esc(short(r.url,70))}`).join('\n\n'),{inline_keyboard:keys})}
async function showHub(token,chatId,messageId,env,origin){const rows=(await env.DB.prepare("SELECT title,type,creator_name FROM hub_resources WHERE status='published' ORDER BY updated_at DESC LIMIT 8").all()).results||[];return edit(token,chatId,messageId,'<b>TAVO HUB / LATEST</b>\n\n'+(rows.length?rows.map(r=>`• <b>${esc(short(r.title,42))}</b> · ${esc(r.type)}${r.creator_name?` · ${esc(r.creator_name)}`:''}`).join('\n'):'No resources yet.'),{inline_keyboard:[[urlButton('OPEN HUB ↗',origin+'/hub.html')],[button('HOME','adm:home')]]})}
async function showAuthors(token,chatId,messageId,env){const rows=(await env.DB.prepare("SELECT creator_name,COUNT(*) n FROM hub_resources WHERE status='published' AND creator_name!='' GROUP BY creator_name ORDER BY n DESC,creator_name LIMIT 12").all()).results||[];return edit(token,chatId,messageId,'<b>AUTHORS</b>\n\n'+(rows.length?rows.map(r=>`${r.n} · ${esc(r.creator_name)}`).join('\n'):'No creator data yet.'),{inline_keyboard:[[button('HOME','adm:home')]]})}
async function showIssues(token,chatId,messageId,env){const one=async sql=>Number((await env.DB.prepare(sql).first())?.n||0),[noCreator,noFiles,noMedia,other]=await Promise.all([one("SELECT COUNT(*) n FROM hub_resources WHERE status='published' AND trim(creator_name)=''"),one("SELECT COUNT(*) n FROM hub_resources r WHERE status='published' AND NOT EXISTS(SELECT 1 FROM hub_resource_files f WHERE f.resource_id=r.id AND f.name NOT LIKE '__extra__%')"),one("SELECT COUNT(*) n FROM hub_resources WHERE status='published' AND (media='[]' OR media='' OR media IS NULL)"),one("SELECT COUNT(*) n FROM hub_resources WHERE status='published' AND type='other'")]);return edit(token,chatId,messageId,`<b>HUB ISSUES</b>\n\nNO CREATOR: ${noCreator}\nNO DOWNLOAD FILE: ${noFiles}\nNO MEDIA: ${noMedia}\nTYPE OTHER: ${other}`,{inline_keyboard:[[button('HOME','adm:home')]]})}

async function handleMessage(token,message,env,origin){
  const chatId=message.chat?.id,uid=String(message.from?.id||''),text=clean(message.text||message.caption);if(uid!==String(env.TELEGRAM_ADMIN_USER_ID||''))return;
  if(text==='/start'||text==='/menu')return send(token,chatId,'<b>ARCHIVE.EXE ADMIN</b>\n\nPrivate control console connected to the site database.',adminMenu());
  const session=await getSession(env,uid),active=session?.draft_id?await getDraft(env,session.draft_id):null,meta=forwardedMeta(message)||parseSourceUrl(extractTelegramUrl(text));
  if(active&&hasDirectAttachment(message)&&!meta){const row=await attachRawFiles(env,active,message);return send(token,chatId,`<b>FILES ADDED TO ACTIVE RESOURCE ✓</b>\n\n${draftText(row,true)}`,activeButtons(row.id,row.source_url))}
  if(meta){
    await send(token,chatId,active?'ADDING TO ACTIVE RESOURCE…':'ANALYZING TELEGRAM POST…');
    let analysis=null;try{analysis=await analyzeUrl(meta.url)}catch{}
    if(!analysis)analysis={ok:true,source:{type:'telegram',url:meta.url,channel:meta.channel,postId:String(meta.postId),rawText:text,author:meta.channel},draft:{title:text&&text.length<120?text:'ATTACHMENTS',creator:meta.channel,creatorLink:`https://t.me/${meta.channel}`,type:'other',models:[],settings:[],descriptionShort:text,descriptionFull:text,tags:[]},media:[],files:[],confidence:{}};
    applyChannelIdentity(analysis,meta.channel);analysis.sourcePosts=[{url:meta.url}];analysis.files=uniqBy([...(analysis.files||[]),...directFiles(message,meta.url)],x=>x.telegram_file_unique_id||x.telegram_file_id||x.url||`${x.name}:${x.size}`);analysis.media=uniqBy([...(analysis.media||[]),...directMedia(message,meta.url)],x=>x.url||x.telegram_file_unique_id||x.telegram_file_id);
    if(active){const row=await mergeIntoDraft(env,active,analysis,message,meta);await setSession(env,uid,row.id,session.channel||meta.channel,meta.postId);return send(token,chatId,`<b>ATTACHED TO ACTIVE RESOURCE ✓</b>\n\n${draftText(row,true)}`,activeButtons(row.id,row.source_url))}
    const id=await saveDraft(env,analysis),row=await getDraft(env,id);await setSession(env,uid,id,meta.channel,meta.postId);return send(token,chatId,draftText(row,true),activeButtons(id,row.source_url));
  }
  if(active)return send(token,chatId,'<b>ACTIVE RESOURCE IS STILL OPEN</b>\n\nForward more Telegram posts or send files/photos here and they will be attached to this draft. When this resource is complete, press FINISH RESOURCE. Use NEW RESOURCE before starting a different one.',activeButtons(active.id,active.source_url));
  if(text)return send(token,chatId,'Send the first Telegram post of a resource. After that, every forwarded post or file you send is attached to the same active draft until you press FINISH RESOURCE or NEW RESOURCE.',adminMenu());
}

async function handleCallback(token,q,env,origin){const uid=String(q.from?.id||''),chatId=q.message?.chat?.id,messageId=q.message?.message_id,data=clean(q.data);if(uid!==String(env.TELEGRAM_ADMIN_USER_ID||'')){await answerCb(token,q.id,'Access denied');return}await answerCb(token,q.id);if(data==='noop')return;
  if(data==='adm:home')return edit(token,chatId,messageId,'<b>ARCHIVE.EXE ADMIN</b>\n\nPrivate control console connected to the site database.',adminMenu());
  if(data==='adm:import'){await clearSession(env,uid);return edit(token,chatId,messageId,'<b>IMPORT RESOURCE</b>\n\nSend the first Telegram post. From that moment the resource stays ACTIVE: any next forwarded posts, photos or files are attached to the same draft. There is no timer and post numbers do not matter. Press FINISH RESOURCE when done.',{inline_keyboard:[[button('HOME','adm:home')]]})}
  if(data==='session:finish'){const s=await getSession(env,uid),row=s?.draft_id?await getDraft(env,s.draft_id):null;await clearSession(env,uid);return row?edit(token,chatId,messageId,`<b>RESOURCE FINISHED ✓</b>\n\n${draftText(row,false)}`,idleDraftButtons(row.id,row.source_url)):edit(token,chatId,messageId,'No active resource.',adminMenu())}
  if(data==='session:new'){await clearSession(env,uid);return edit(token,chatId,messageId,'<b>NEW RESOURCE</b>\n\nSend its first Telegram post. Everything after it will attach to that draft until you press FINISH RESOURCE.',{inline_keyboard:[[button('HOME','adm:home')]]})}
  if(data.startsWith('session:resume:')){const id=data.slice(15),row=await getDraft(env,id);if(!row)return edit(token,chatId,messageId,'Draft not found.',adminMenu());const m=parseSourceUrl(row.source_url);await setSession(env,uid,id,m?.channel||row.analysis?.source?.channel||'',m?.postId||0);return edit(token,chatId,messageId,draftText(row,true),activeButtons(id,row.source_url))}
  if(data==='adm:drafts')return showDraftList(token,chatId,messageId,env);if(data==='adm:suggestions')return showSuggestions(token,chatId,messageId,env);if(data==='adm:hub')return showHub(token,chatId,messageId,env,origin);if(data==='adm:authors')return showAuthors(token,chatId,messageId,env);if(data==='adm:issues')return showIssues(token,chatId,messageId,env);if(data==='adm:stats')return edit(token,chatId,messageId,await adminStats(env),{inline_keyboard:[[button('HOME','adm:home')]]});
  if(data.startsWith('draft:view:')){const row=await getDraft(env,data.slice(11));return row?edit(token,chatId,messageId,draftText(row,false),idleDraftButtons(row.id,row.source_url)):edit(token,chatId,messageId,'Draft not found.',adminMenu())}
  if(data.startsWith('draft:reanalyze:')){const id=data.slice(16),row=await getDraft(env,id);if(!row)return;try{const fresh=await analyzeUrl(row.source_url),old=row.analysis||{};applyChannelIdentity(fresh,old.source?.channel||fresh.source?.channel);fresh.sourcePosts=old.sourcePosts||[{url:row.source_url}];fresh.files=uniqBy([...(fresh.files||[]),...(old.files||[])],x=>x.telegram_file_unique_id||x.telegram_file_id||x.url||`${x.name}:${x.size}`);fresh.media=uniqBy([...(fresh.media||[]),...(old.media||[])],x=>x.telegram_file_unique_id||x.telegram_file_id||x.url);await env.DB.prepare("UPDATE telegram_admin_drafts SET payload=?,status='review',updated_at=CURRENT_TIMESTAMP WHERE id=?").bind(JSON.stringify(fresh),id).run();const r=await getDraft(env,id),s=await getSession(env,uid),active=s?.draft_id===id;return edit(token,chatId,messageId,draftText(r,active),active?activeButtons(id,r.source_url):idleDraftButtons(id,r.source_url))}catch(e){return edit(token,chatId,messageId,`<b>RE-ANALYZE FAILED</b>\n${esc(e.message||e)}`,idleDraftButtons(id,row.source_url))}}
  if(data.startsWith('draft:publish:')){const id=data.slice(14),row=await getDraft(env,id);if(!row)return;try{const out=await publishDraft(token,env,row);const s=await getSession(env,uid);if(s?.draft_id===id)await clearSession(env,uid);return edit(token,chatId,messageId,`<b>PUBLISHED ✓</b>\n\n${esc(row.analysis?.draft?.title||'Resource')}\nID: ${esc(out.id||'')}`,{inline_keyboard:[[urlButton('OPEN HUB ↗',origin+'/hub.html')],[button('DRAFTS','adm:drafts'),button('HOME','adm:home')]]})}catch(e){return edit(token,chatId,messageId,`<b>PUBLISH FAILED</b>\n\n${esc(e.message||e)}`,idleDraftButtons(id,row.source_url))}}
  if(data.startsWith('draft:delete:')){const id=data.slice(13),s=await getSession(env,uid);if(s?.draft_id===id)await clearSession(env,uid);await env.DB.prepare('DELETE FROM telegram_admin_drafts WHERE id=?').bind(id).run();return showDraftList(token,chatId,messageId,env)}
  if(data.startsWith('sug:ignore:')){const id=Number(data.slice(11));await env.DB.prepare("UPDATE hub_suggestions SET status='ignored',updated_at=CURRENT_TIMESTAMP WHERE id=?").bind(id).run();return showSuggestions(token,chatId,messageId,env)}
  if(data.startsWith('sug:import:')){const sid=Number(data.slice(11)),s=await env.DB.prepare('SELECT id,url FROM hub_suggestions WHERE id=? LIMIT 1').bind(sid).first();if(!s)return showSuggestions(token,chatId,messageId,env);try{const a=await analyzeUrl(s.url),m=parseSourceUrl(s.url);if(m)applyChannelIdentity(a,m.channel);const id=await saveDraft(env,a);await env.DB.prepare("UPDATE hub_suggestions SET status='reviewing',updated_at=CURRENT_TIMESTAMP WHERE id=?").bind(sid).run();const row=await getDraft(env,id);return edit(token,chatId,messageId,draftText(row,false),idleDraftButtons(id,row.source_url))}catch(e){return edit(token,chatId,messageId,`<b>SUGGESTION IMPORT FAILED</b>\n\n${esc(e.message||e)}`,{inline_keyboard:[[button('← SUGGESTIONS','adm:suggestions')]]})}}
}

export async function handleAdminTelegram(request,env){if(request.method!=='POST')return json({ok:false,error:'METHOD_NOT_ALLOWED'},405);await ensureSchema(env);const token=clean(env.Node00admin);if(!token)return json({ok:false,error:'ADMIN_BOT_TOKEN_MISSING'},503);if(request.headers.get('x-telegram-bot-api-secret-token')!==await webhookSecret(token))return json({ok:false,error:'INVALID_WEBHOOK_SECRET'},403);let u={};try{u=await request.json()}catch{return json({ok:false,error:'INVALID_JSON'},400)}const origin=new URL(request.url).origin;try{if(u.callback_query)await handleCallback(token,u.callback_query,env,origin);else if(u.message)await handleMessage(token,u.message,env,origin)}catch(e){console.error('admin telegram error',e);try{const chatId=u.message?.chat?.id||u.callback_query?.message?.chat?.id;if(chatId)await send(token,chatId,`<b>ADMIN BOT ERROR</b>\n${esc(e.message||e)}`,adminMenu())}catch{}}return json({ok:true})}
