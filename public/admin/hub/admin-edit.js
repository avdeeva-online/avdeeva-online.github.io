(()=>{
  const $=s=>document.querySelector(s);
  const allowedSettings=new Set(['modern','fantasy','medieval','post-apocalypse','sci-fi','omegaverse','rusreal']);
  const normalizeSettings=arr=>[...new Set((Array.isArray(arr)?arr:[]).map(x=>{x=String(x).toLowerCase();if(x==='historical')return'medieval';if(x==='magic')return'fantasy';return x}).filter(x=>allowedSettings.has(x)))];
  const isImageFile=f=>/^image\//i.test(String(f?.type||f?.mime||''))||/\.(png|jpe?g|webp|gif)$/i.test(String(f?.name||''));
  const isExtraStored=f=>Boolean(f?.extra)||String(f?.name||'').startsWith('__extra__');
  const cleanExtraName=v=>String(v||'').replace(/^__extra__/,'');
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  let editResource=null,pendingFiles=[],extraPendingImages=[],dirty=false,loadingResource=false;

  function setStatus(text,kind=''){const e=$('#analyzeStatus');if(!e)return;e.textContent=text;e.className='status '+kind}
  function markDirty(){if(!loadingResource)dirty=true}
  function selected(boxSelector){return[...document.querySelectorAll(`${boxSelector} .choice.active`)].map(b=>b.dataset.v)}
  function choose(boxSelector,values){const box=$(boxSelector);if(!box)return;const wanted=new Set(boxSelector==='#settingChoices'?normalizeSettings(values):(Array.isArray(values)?values:[values]).filter(Boolean));box.querySelectorAll('.choice').forEach(b=>{const should=wanted.has(b.dataset.v),active=b.classList.contains('active');if(should!==active)b.click()})}
  function syncSettingChoices(){const box=$('#settingChoices');if(!box)return;['historical','magic'].forEach(v=>box.querySelector(`.choice[data-v="${v}"]`)?.remove());if(!box.querySelector('.choice[data-v="rusreal"]')){const b=document.createElement('button');b.type='button';b.className='choice';b.dataset.v='rusreal';b.textContent='RUSREAL';box.appendChild(b)}}
  syncSettingChoices();

  const full=$('#fullDesc');
  if(full&&!$('#additionalInfo')){const wrap=document.createElement('div');wrap.className='field';wrap.innerHTML='<label>ADDITIONAL INFO / AUTHOR NOTES</label><textarea id="additionalInfo" placeholder="Дополнительные настройки, пояснения автора, заметки, ссылки и другая информация для вкладки EXTRAS."></textarea>';full.closest('.field')?.insertAdjacentElement('afterend',wrap)}
  const fileField=$('#fileDrop')?.closest('.field');
  if(fileField){
    if(!$('#extraImageInput')){const extra=document.createElement('div');extra.className='field';extra.dataset.extraGallery='1';extra.innerHTML='<label>EXTRAS / GALLERY IMAGES</label><input id="extraImageInput" type="file" accept="image/png,image/jpeg,image/webp" multiple hidden><div id="extraImageDrop" class="file-drop" tabindex="0"><b>ADD EXTRA IMAGES</b><span>JPG, PNG, WEBP · optimized · shown only inside EXTRAS</span></div><div id="extraImageList" class="file-list"></div><div id="extraImageSummary" class="file-summary">NO EXTRA IMAGES ATTACHED</div>';fileField.insertAdjacentElement('afterend',extra)}
    if(!$('#existingFiles')){const box=document.createElement('div');box.id='existingFiles';box.style.marginTop='10px';fileField.appendChild(box)}
  }

  function pendingKey(f){return`${f?.name||''}|${Number(f?.size||0)}|${Number(f?.lastModified||0)}`}
  function rememberPendingFiles(list){for(const f of Array.from(list||[])){if(!(f instanceof File))continue;const key=pendingKey(f),i=pendingFiles.findIndex(x=>pendingKey(x)===key);if(i<0)pendingFiles.push(f)}markDirty()}
  $('#fileInput')?.addEventListener('change',e=>rememberPendingFiles(e.target.files),true);
  $('#fileDrop')?.addEventListener('drop',e=>rememberPendingFiles(e.dataTransfer?.files),true);
  $('#fileList')?.addEventListener('click',e=>{const remove=e.target.closest('[data-remove]');if(remove){const i=Number(remove.dataset.remove);if(Number.isInteger(i)&&i>=0&&i<pendingFiles.length)pendingFiles.splice(i,1);markDirty()}},true);

  function renderExtraPending(){const box=$('#extraImageList'),summary=$('#extraImageSummary');if(!box||!summary)return;if(!extraPendingImages.length){box.innerHTML='';summary.textContent='NO EXTRA IMAGES ATTACHED';return}box.innerHTML=extraPendingImages.map((f,i)=>`<div class="file-row"><div><div class="file-name">${esc(f.name)}</div><div class="file-meta">EXTRAS IMAGE · ${(Math.max(0,Number(f.size)||0)/1024).toFixed(0)} KB</div></div><button class="file-action remove" type="button" data-remove-extra="${i}">REMOVE</button></div>`).join('');summary.textContent=`${extraPendingImages.length} EXTRA IMAGE${extraPendingImages.length===1?'':'S'} READY`;box.querySelectorAll('[data-remove-extra]').forEach(b=>b.onclick=()=>{extraPendingImages.splice(Number(b.dataset.removeExtra),1);renderExtraPending();markDirty()})}
  async function canvasBlob(canvas,type,quality){return new Promise(resolve=>canvas.toBlob(resolve,type,quality))}
  async function optimizeExtraImage(file){if(!(file instanceof File)||!isImageFile(file))return null;if(file.type==='image/gif')throw new Error(`${file.name}: GIF is not supported for EXTRAS`);let bitmap;try{bitmap=await createImageBitmap(file)}catch{throw new Error(`${file.name}: image could not be decoded`)}const base=file.name.replace(/\.[^.]+$/,'')||'extra-image';let maxSide=2200,quality=.9,blob=null;for(let attempt=0;attempt<7;attempt++){const scale=Math.min(1,maxSide/Math.max(bitmap.width,bitmap.height)),w=Math.max(1,Math.round(bitmap.width*scale)),h=Math.max(1,Math.round(bitmap.height*scale)),canvas=document.createElement('canvas');canvas.width=w;canvas.height=h;canvas.getContext('2d',{alpha:false}).drawImage(bitmap,0,0,w,h);blob=await canvasBlob(canvas,'image/webp',quality);if(blob&&blob.size<=1200*1024)break;maxSide=Math.max(1200,Math.round(maxSide*.88));quality=Math.max(.68,quality-.04)}bitmap.close?.();if(!blob||blob.size>1500*1024)throw new Error(`${file.name}: image is still too large after optimization`);return new File([blob],`${base}.webp`,{type:'image/webp',lastModified:Date.now()})}
  async function rememberExtraImages(list){const incoming=Array.from(list||[]).filter(f=>f instanceof File&&isImageFile(f));if(!incoming.length)return;setStatus('PREPARING EXTRA IMAGES...');try{for(const original of incoming){const f=await optimizeExtraImage(original);if(!f)continue;const i=extraPendingImages.findIndex(x=>x.name===f.name);if(i>=0)extraPendingImages[i]=f;else extraPendingImages.push(f)}renderExtraPending();markDirty();setStatus('EXTRA IMAGES READY.','ok')}catch(e){setStatus('EXTRA IMAGE ERROR: '+e.message,'bad')}}
  $('#extraImageDrop')?.addEventListener('click',()=>$('#extraImageInput')?.click());
  $('#extraImageDrop')?.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();$('#extraImageInput')?.click()}});
  $('#extraImageInput')?.addEventListener('change',async e=>{await rememberExtraImages(e.target.files);e.target.value=''});
  $('#extraImageDrop')?.addEventListener('dragover',e=>{e.preventDefault();e.currentTarget.classList.add('drag')});
  $('#extraImageDrop')?.addEventListener('dragleave',e=>e.currentTarget.classList.remove('drag'));
  $('#extraImageDrop')?.addEventListener('drop',async e=>{e.preventDefault();e.currentTarget.classList.remove('drag');await rememberExtraImages(e.dataTransfer?.files)});

  function primaryPendingIndex(){const b=$('#fileList .file-action.primary[data-primary]');const i=Number(b?.dataset.primary);return Number.isInteger(i)&&i>=0?i:(pendingFiles.length?0:-1)}
  function collectMedia(){const manual=typeof window.archiveHubGetManualCover==='function'?String(window.archiveHubGetManualCover()||''):'';const nodes=[...document.querySelectorAll('#mediaGrid .media img')],coverNode=document.querySelector('#mediaGrid .media.cover img');let media=nodes.map(img=>({url:img.currentSrc||img.src||'',cover:img===coverNode})).filter(x=>x.url);if(editResource&&media.length===0)media=Array.isArray(editResource.media)?editResource.media.map(x=>({...x})):[];if(manual){media=media.filter(x=>!x?.manual_cover&&x?.url!==manual).map(x=>({...x,cover:false}));media.unshift({url:manual,cover:true,manual_cover:true})}else if(media.length&&!media.some(x=>x.cover))media[0].cover=true;return media}
  function draft(){const primary=primaryPendingIndex();return{editing_id:editResource?.id||'',source:{type:'telegram',url:$('#postUrl')?.value?.trim()||editResource?.source_url||''},type:selected('#typeChoices')[0]||editResource?.type||'other',title:$('#title')?.value?.trim()||'',creator:{name:$('#creator')?.value?.trim()||'',link:$('#creatorLink')?.value?.trim()||''},description_short:$('#shortDesc')?.value?.trim()||'',description_full:$('#fullDesc')?.value?.trim()||'',additional_info:$('#additionalInfo')?.value?.trim()||'',models:selected('#modelChoices'),settings:normalizeSettings(selected('#settingChoices')),tags:($('#extraTags')?.value||'').split(',').map(x=>x.trim()).filter(Boolean),media:collectMedia(),files:pendingFiles.map((f,i)=>({name:f.name,size:f.size,type:f.type||'',primary:i===primary,source:'manual'})),confidence:editResource?.confidence||{},status:'published'}}
  async function parseApiResponse(r,label){const raw=await r.text();let x={};try{x=JSON.parse(raw||'{}')}catch{}if(!r.ok||!x.ok)throw new Error(`${label}: ${[x.error||`HTTP_${r.status}`,x.detail].filter(Boolean).join(': ')}`);return x}
  async function postResourceJson(d,label='RESOURCE METADATA',action=''){let r;try{r=await fetch('/api/admin/hub-resource'+(action?`?action=${encodeURIComponent(action)}`:''),{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(d)})}catch(e){throw new Error(`${label}: NETWORK ERROR (${e.message||'Failed to fetch'})`)}return parseApiResponse(r,label)}
  async function postOneFile(d,file,key,label,session){const fd=new FormData();fd.append('resource',new Blob([JSON.stringify({...d,_publish_session:session})],{type:'application/json'}),'resource.json');fd.append(key,file,file.name);let r;try{r=await fetch('/api/admin/hub-resource?action=upload',{method:'POST',body:fd})}catch(e){throw new Error(`${label}: NETWORK ERROR (${e.message||'Failed to fetch'})`)}return parseApiResponse(r,label)}
  async function sendResource(d){
    const primary=primaryPendingIndex(),normalBytes=pendingFiles.reduce((n,f)=>n+Number(f.size||0),0),extraBytes=extraPendingImages.reduce((n,f)=>n+Number(f.size||0),0);
    if(pendingFiles.some(f=>f.size>10*1024*1024)||normalBytes>25*1024*1024)throw new Error('FILES TOO LARGE: 10 MB PER FILE / 25 MB TOTAL');
    if(extraPendingImages.some(f=>f.size>4*1024*1024)||extraBytes>12*1024*1024)throw new Error('EXTRAS TOO LARGE: 4 MB PER IMAGE / 12 MB TOTAL');
    const base={...d,files:[]};
    setStatus('STARTING SAFE PUBLISH...');
    const first=await postResourceJson(base,'PUBLISH BEGIN','begin'),session=first.session_id,id=first.id||editResource?.id||'';
    if(!session)throw new Error('PUBLISH BEGIN: SESSION MISSING');
    let total=pendingFiles.length+extraPendingImages.length,done=0;
    try{
      for(let i=0;i<pendingFiles.length;i++){
        const f=pendingFiles[i],uploadDraft={...base,editing_id:id,files:[{name:f.name,size:f.size,type:f.type||'',primary:i===primary,source:'manual'}]};
        setStatus(`UPLOADING FILE ${++done}/${total}: ${f.name}`);
        await postOneFile(uploadDraft,f,'files',`FILE ${f.name}`,session);
      }
      for(const f of extraPendingImages){
        const uploadDraft={...base,editing_id:id,files:[]};
        setStatus(`UPLOADING EXTRA ${++done}/${total}: ${f.name}`);
        await postOneFile(uploadDraft,f,'extraImages',`EXTRA ${f.name}`,session);
      }
      setStatus('FINALIZING RESOURCE...');
      const last=await postResourceJson({_publish_session:session},'FINALIZE','finalize');
      return{...last,id:last.id||id,updated:Boolean(first.updated||editResource)};
    }catch(e){try{await postResourceJson({_publish_session:session},'ROLLBACK','cancel')}catch{}throw e}
  }
  async function reloadResource(id){const r=await fetch('/api/hub-resources',{cache:'no-store'}),d=await r.json().catch(()=>({}));if(!r.ok||!d?.ok)throw new Error(d?.error||`HTTP_${r.status}`);const item=(d.resources||[]).find(x=>String(x.id)===String(id));if(!item)throw new Error('RESOURCE_NOT_FOUND_AFTER_UPDATE');return item}
  async function publishResource(){const d=draft();if(!d.source.url||!d.title||!d.type){setStatus('SOURCE URL, TITLE AND TYPE ARE REQUIRED.','bad');return}const b=$('#publish');if(b)b.disabled=true;setStatus(editResource?'UPDATING RESOURCE...':'PUBLISHING RESOURCE...');try{const x=await sendResource(d);pendingFiles=[];extraPendingImages=[];renderExtraPending();$('#fileList')&&( $('#fileList').innerHTML='');$('#fileSummary')&&( $('#fileSummary').textContent='NO NEW DOWNLOAD FILES ATTACHED');dirty=false;try{localStorage.removeItem('archiveHubDraft:'+d.source.url)}catch{}if(editResource){const fresh=await reloadResource(x.id||editResource.id);fillResource(fresh);setStatus('RESOURCE UPDATED SUCCESSFULLY.','ok')}else{setStatus('PUBLISHED. RESOURCE ID: '+(x.id||'OK'),'ok')}}catch(e){setStatus((editResource?'UPDATE':'PUBLISH')+' FAILED: '+e.message,'bad')}finally{if(b)b.disabled=false}}
  function saveLocalDraft(){const d=draft();if(!d.source.url){setStatus('ADD A TELEGRAM URL FIRST.','bad');return}try{localStorage.setItem('archiveHubDraft:'+d.source.url,JSON.stringify(d));setStatus((pendingFiles.length||extraPendingImages.length)?'DRAFT METADATA SAVED. LOCAL FILE BYTES STAY ATTACHED ONLY WHILE THIS PAGE IS OPEN.':'DRAFT SAVED LOCALLY.','ok')}catch(e){setStatus('DRAFT SAVE FAILED: '+e.message,'bad')}}

  function renderStoredMedia(media){const grid=$('#mediaGrid'),items=Array.isArray(media)?media:[];if(!grid)return;if(!items.length){grid.innerHTML='<div class="empty">NO MEDIA DETECTED · use MANUAL COVER if needed</div>';return}let coverIndex=items.findIndex(x=>x?.cover);if(coverIndex<0)coverIndex=0;grid.innerHTML=items.map((m,i)=>{const src=m?.url||m?.src||'';return src?`<div class="media ${i===coverIndex?'cover':''}"><img src="${esc(src)}" alt="Resource media ${i+1}" loading="lazy"><button type="button" data-admin-cover="${i}">${i===coverIndex?'COVER':'SET COVER'}</button></div>`:''}).join('');grid.querySelectorAll('[data-admin-cover]').forEach(btn=>btn.onclick=()=>{grid.querySelectorAll('.media').forEach((m,i)=>m.classList.toggle('cover',i===Number(btn.dataset.adminCover)));grid.querySelectorAll('[data-admin-cover]').forEach((b,i)=>b.textContent=i===Number(btn.dataset.adminCover)?'COVER':'SET COVER');markDirty()})}
  function renderExistingFiles(){const box=$('#existingFiles');if(!box)return;const files=Array.isArray(editResource?.files)?editResource.files:[];if(!files.length){box.innerHTML=editResource?'<div class="file-summary">NO EXISTING FILES OR EXTRA IMAGES</div>':'';return}const downloads=files.filter(f=>!isExtraStored(f)),extras=files.filter(isExtraStored);const rows=(items,extra=false)=>items.map(f=>`<div class="file-row" data-existing-id="${esc(f.id)}"><div><div class="file-name">${esc(cleanExtraName(f.name||'FILE'))}</div><div class="file-meta">${extra?'EXTRAS IMAGE':'DOWNLOAD FILE'} · ${esc(f.mime||'FILE')} · ${Math.max(0,Number(f.size)||0)} B${!extra&&f.primary?' · PRIMARY':''}</div></div>${!extra&&!f.primary?`<button class="file-action" type="button" data-primary-file="${esc(f.id)}">MAKE PRIMARY</button>`:''}<a class="file-action" href="${esc(f.download_url)}" target="_blank" rel="noopener">OPEN</a><button class="file-action remove" type="button" data-delete-file="${esc(f.id)}">REMOVE</button></div>`).join('');box.innerHTML=`<div class="file-summary" style="margin-bottom:6px">EXISTING DOWNLOAD FILES</div>${downloads.length?rows(downloads):'<div class="file-summary">NONE</div>'}<div class="file-summary" style="margin:12px 0 6px">EXISTING EXTRAS IMAGES · displayed in EXTRAS only</div>${extras.length?rows(extras,true):'<div class="file-summary">NONE</div>'}`;box.querySelectorAll('[data-primary-file]').forEach(btn=>btn.onclick=async()=>{btn.disabled=true;try{const r=await fetch(`/api/admin/hub-resource/${encodeURIComponent(editResource.id)}/files/${encodeURIComponent(btn.dataset.primaryFile)}/primary`,{method:'POST'}),d=await r.json().catch(()=>({}));if(!r.ok||!d.ok)throw new Error(d.error||`HTTP_${r.status}`);editResource=await reloadResource(editResource.id);renderExistingFiles();setStatus('PRIMARY DOWNLOAD UPDATED.','ok')}catch(e){setStatus('PRIMARY UPDATE FAILED: '+e.message,'bad');btn.disabled=false}});box.querySelectorAll('[data-delete-file]').forEach(btn=>btn.onclick=async()=>{if(!confirm('Remove this item from the resource?'))return;btn.disabled=true;try{const r=await fetch(`/api/admin/hub-resource/${encodeURIComponent(editResource.id)}/files/${encodeURIComponent(btn.dataset.deleteFile)}`,{method:'DELETE'}),d=await r.json().catch(()=>({}));if(!r.ok||!d.ok)throw new Error(d.error||`HTTP_${r.status}`);editResource.files=editResource.files.filter(f=>String(f.id)!==String(btn.dataset.deleteFile));renderExistingFiles();setStatus('ITEM REMOVED.','ok')}catch(e){setStatus('REMOVE FAILED: '+e.message,'bad');btn.disabled=false}})}
  function fillResource(r){loadingResource=true;editResource=r;$('#postUrl').value=r.source_url||'';$('#rawText').value=r.description_full||'';$('#sourceLink').textContent=r.source_url||'';$('#title').value=r.title||'';$('#creator').value=r.creator?.name||'';$('#creatorLink').value=r.creator?.link||'';$('#shortDesc').value=r.description_short||'';$('#fullDesc').value=r.description_full||'';$('#additionalInfo').value=r.additional_info||'';$('#extraTags').value=(r.tags||[]).join(', ');choose('#typeChoices',r.type);choose('#modelChoices',r.models||[]);choose('#settingChoices',normalizeSettings(r.settings||[]));renderStoredMedia(r.media||[]);renderExistingFiles();renderExtraPending();const pub=$('#publish');if(pub)pub.textContent='UPDATE RESOURCE';document.querySelector('h1')&&(document.querySelector('h1').textContent='EDIT TAVO HUB RESOURCE');setStatus('EDIT MODE · '+(r.title||r.id),'ok');pendingFiles=[];dirty=false;loadingResource=false}
  async function initEdit(){const id=new URLSearchParams(location.search).get('edit');if(!id)return;setStatus('LOADING RESOURCE...');try{const item=await reloadResource(id);fillResource(item)}catch(e){setStatus('EDIT LOAD FAILED: '+e.message,'bad')}}

  const originalReanalyze=$('#reanalyze')?.onclick;
  if(originalReanalyze)$('#reanalyze').onclick=async e=>{const snapshot={title:$('#title').value,creator:$('#creator').value,creatorLink:$('#creatorLink').value,short:$('#shortDesc').value,full:$('#fullDesc').value,additional:$('#additionalInfo').value,tags:$('#extraTags').value,type:selected('#typeChoices'),models:selected('#modelChoices'),settings:selected('#settingChoices')};await originalReanalyze.call($('#reanalyze'),e);$('#title').value=snapshot.title;$('#creator').value=snapshot.creator;$('#creatorLink').value=snapshot.creatorLink;$('#shortDesc').value=snapshot.short;$('#fullDesc').value=snapshot.full;$('#additionalInfo').value=snapshot.additional;$('#extraTags').value=snapshot.tags;choose('#typeChoices',snapshot.type);choose('#modelChoices',snapshot.models);choose('#settingChoices',snapshot.settings);setStatus('RE-ANALYZED · MANUAL EDITS PRESERVED.','ok')};
  $('#publish')&&( $('#publish').onclick=publishResource );
  $('#saveDraft')&&( $('#saveDraft').onclick=saveLocalDraft );
  ['#title','#creator','#creatorLink','#shortDesc','#fullDesc','#additionalInfo','#extraTags','#postUrl'].forEach(sel=>$(sel)?.addEventListener('input',markDirty));
  ['#typeChoices','#modelChoices','#settingChoices'].forEach(sel=>$(sel)?.addEventListener('click',markDirty));
  window.addEventListener('archive:hub-source-media-change',markDirty);
  window.addEventListener('beforeunload',e=>{if(!dirty)return;e.preventDefault();e.returnValue=''});
  renderExtraPending();initEdit();
})();
