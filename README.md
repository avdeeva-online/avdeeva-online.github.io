# ARCHIVE.EXE v0.9.25 — Hero No-Loop

- Rebuilt the hero asset so the terminal is physically on the left side of the image.
- Removed the previous fake terminal-copy relocation that looked like an unidentified object.
- Removed synchronized looping background / haze animations.
- Background drift is now continuous time-based motion with no keyframe reset.
- Dust is now made of independent falling particles with random respawn times.
- Deer has a separate feathered moving layer with continuous 3–5 px micro-motion.
- Terminal wakes/glitches at random intervals instead of repeating on a fixed loop.
- Catalog, toolbar, filters and existing anomaly/easter-egg logic are otherwise unchanged.
